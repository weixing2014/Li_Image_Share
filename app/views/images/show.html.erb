<%= link_to 'Go Back', images_path %>

<br>

<%= link_to 'Delete the Image', image_path(@image), method: :delete,
            data: { confirm: "Are you sure to delete this image?" } %>

<hr>

<%= image_tag(@image.url) %>

<hr>

<%= render_tag_links(@image.tag_list) %>
